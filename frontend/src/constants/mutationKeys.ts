/** Shared TanStack Query `mutationKey` values — use with `useIsMutating` / guards. */
export const TASKS_CREATE_MUTATION_KEY = ['tasks', 'create'] as const;
export const TASKS_PATCH_MUTATION_KEY = ['tasks', 'patch'] as const;
export const TASKS_DELETE_MUTATION_KEY = ['tasks', 'delete'] as const;
