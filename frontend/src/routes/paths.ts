/**
 * Central route paths — change here when adding pages (Layout, links, router stay in sync).
 */
export const ROUTES = {
  HOME: '/',
  API_DEMO: '/api-demo',
  BLOCKCHAIN_DEMO: '/blockchain-demo',
} as const;
