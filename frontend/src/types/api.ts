/**
 * Shared API types from OpenAPI codegen (single source of truth).
 * @see docs/SWAGGER.md
 */

export type {
  AuthResponse,
  Task,
  TaskListResponse,
  CreateTaskRequest,
  CredentialRequest,
  PatchTaskRequest,
  ErrorResponse,
  HealthResponse,
} from '../api/openapi';
