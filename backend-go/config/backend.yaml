# backend-go — no secrets (use env / .env for AUTH_JWT_SECRET, PORT, etc.).
server:
  port: 3001
  mode: debug

cors:
  origins:
    - "*"

database:
  # Monorepo root: same file as backend-ts (`store/` next to `docs/`).
  path: store/app.sqlite
